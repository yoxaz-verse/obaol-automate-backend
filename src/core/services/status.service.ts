import { GenericRepository } from "../repositories/generic.repository"; // Import GenericRepository
import { Types } from "mongoose";

export class StatusService {
    // This service previously handled Project and Activity status orchestration.
    // Those entities have been removed as per user request.
    // Keeping the class for future generic status logic if needed.

    constructor() { }
}
